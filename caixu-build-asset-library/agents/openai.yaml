interface:
  display_name: "Caixu Asset Library"
  short_description: "Extract asset cards and conservative merge groups from parsed files"
  default_prompt: "Use $caixu-build-asset-library to read parsed files, extract canonical asset cards from trustworthy text, persist them, and only merge versions when the evidence is strong."
