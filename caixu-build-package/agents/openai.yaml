interface:
  display_name: "Caixu Package"
  short_description: "Build ledgers and a truthful submission bundle from validated lifecycle data"
  default_prompt: "Use $caixu-build-package to load the latest validated lifecycle run, let the agent choose assets, run docgen deterministically, and persist a package plan without changing readiness."
