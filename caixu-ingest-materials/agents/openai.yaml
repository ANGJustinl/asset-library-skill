interface:
  display_name: "Caixu Ingest"
  short_description: "Import local material files into a Caixu library"
  default_prompt: "Use $caixu-ingest-materials to create or load a library, parse concrete local file paths through the OCR MCP, persist parsed_files, and stop cleanly on full failure."
